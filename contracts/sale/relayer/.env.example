# Relayer HTTP (local). Never commit real keys.
PORT=8787
# SALE_MODE=private
# Public LBP relayer (separate process):
# SALE_MODE=public PORT=8788 PUBLIC_VAULT_ADDRESS=0x…
# EAV7_RPC=http://127.0.0.1:6070
# VAULT_ADDRESS=0x…          # SaleVault (private)
# PUBLIC_VAULT_ADDRESS=0x…   # PublicVault (public)
# RELAYER_PRIVATE_KEY=0x…    # vault relayer only — never Anchor / cold keys
# SALE_OPS_TOKEN=…           # required for HTTP POST /confirm (ops only)
# ETH_RPC=https://…
# BSC_RPC=https://…
# TRON_API=https://api.trongrid.io
# BTC_USD=95000
# POLL_MS=15000
